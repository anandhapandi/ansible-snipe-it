---
- name: Curl
  shell: sudo apt install curl git -y

- name: To download composer 
  shell: curl -sS https://getcomposer.org/installer | sudo php -- --install-dir=/usr/local/bin --filename=composer

